{
  "name": "reduxtheseries",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "npm-run-all": "^3.1.0",
    "react-scripts": "0.4.1"
  },
  "dependencies": {
    "isomorphic-fetch": "^2.2.1",
    "json-server": "^0.8.21",
    "react": "^15.3.1",
    "react-dom": "^15.3.1",
    "react-router": "^2.8.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "dev-api": "json-server --watch api/db.json --routes api/routes.json --port 5000",
    "run-all": "npm-run-all --parallel dev-api start"
  },
  "eslintConfig": {
    "extends": "./node_modules/react-scripts/config/eslint.js"
  }
}
